.TH man 3 "26 October 2020" "0.01" "_printf man page".
.SH NAME
.B _printf.
-  the printf function created by Nicolas Herrera and David Alzate TeamF.
.SH SYNOPSIS
.IR - _printf FORMAT [ARGUMENT].

.IR      _printf OPTION.

.SH DESCRIPTION
 Print ARGUMENT(s) according to FORMAT, or execute according to OPTION:

       --help display this help and exit

       --version
              output version information and exit.
.SH RETURN VALUE
The _printf() function produces output according to a format which is described, returns, if the returns its successful returns the numbers of characters printed.

.SH Format
% indicates which form should be printed.
.SH Flags
.SH %c
Print a character.
.SH %s
Print a string.
.SH %d
Print a decimal number.
.SH %i
Print a single integer.
.SH EXAMPLES
_printf(“This is my age %d”, n);

.SH BUGS
When you write two(2) percent(%) one is printed.
When you write two(2) percent(%) and one(1) operator only print percent and operator.
When you write percent(%) and one invalid operator only print percent and invalid operator.


.PP
David Alzate Alzate - https://github.com/illker
.PP
Nicolas Herrera Castro - https://github.com/nicolasherrerac
